crypto:
  secret-key: ${CRYPTO_SECRET_KEY}
spring:
  application:
    name: customer
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mariadb://localhost:3306/ATF_CUSTOMER?allowMultiQueries=true
    username: atf_customer_admin
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
server:
  port: 8086
  servlet:
    context-path: /customer

logging:
  level:
    jdbc: 'OFF'              # log4jdbc audit/connection/resultset 차단 (HikariCP housekeeper의 Connection.isValid 노이즈 포함)
    jdbc.sqltiming: 'INFO'   # 실행 SQL + 수행시간은 유지